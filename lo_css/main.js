const trigger = document.querySelector('.language-switcher .indicator');const menu = document.querySelector('.language-switcher .menu');let timeout = undefined;let removeTimeout = undefined;function beforeDisplaying(element){element.style.display = 'block';};function afterRemoval(element, transitionDuration){removeTimeout = setTimeout(function(){element.style.display = 'none';}, 300)};function showMenu(){if(timeout){clearTimeout(timeout);clearTimeout(removeTimeout)};beforeDisplaying(menu);setTimeout(() => {menu.classList.add("active");}, 20)};function hideMenu(){timeout = setTimeout(function(){menu.classList.remove("active");afterRemoval(menu, 200)}, 300)};trigger.addEventListener('mouseenter', showMenu);trigger.addEventListener('mouseleave', hideMenu);menu.addEventListener('mouseenter', showMenu);menu.addEventListener('mouseleave', hideMenu); const mobileMenuOpen = document.querySelector('.mobile-menu-button-open');const mobileMenuClose = document.querySelector('.mobile-menu-button-close');const mobileMenu = document.querySelector('.mobile-menu');let mobileMenuTimeout = undefined;let mobileMenuRemoveTimeout = undefined;const mobileMenuHelpers = {beforeDisplaying: function(element) {element.style.display = 'flex';},afterRemoval: function(element, transitionDuration){mobileMenuRemoveTimeout = setTimeout(function(){element.style.display = 'none';}, 300)}};function showMobileMenu(){if(mobileMenuTimeout){clearTimeout(mobileMenuTimeout);clearTimeout(mobileMenuRemoveTimeout)};mobileMenuHelpers.beforeDisplaying(mobileMenu);setTimeout(() => {mobileMenu.classList.add("active");}, 20)};function hideMobileMenu(){mobileMenuTimeout = setTimeout(function(){mobileMenu.classList.remove("active");mobileMenuHelpers.afterRemoval(mobileMenu, 200)},20)}mobileMenuOpen.addEventListener('click', showMobileMenu);mobileMenuClose.addEventListener('click', hideMobileMenu);